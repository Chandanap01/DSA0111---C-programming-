#include<iostream>
using namespace std;
int main()
{
	int i=1,n,sum=0;
	cout<<"enter a number:";
	cin>>n;
	while(i<n)
	{
		if(n%i==0)
		{
			sum=sum+i;	
		}
		i++;
	}
	if(n==sum)
	{
		cout<<"perfect number";
	}
	else
	{
		cout<<"not perfect number";
	}
}
